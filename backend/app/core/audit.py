"""Audit logging helpers over Supabase (scrape_runs, action_audit, notifications_sent)."""

# TODO: write small adapters so every scraper run, every post-approval execution,
# and every notification dispatch writes an audit row. Drives R-APPROVE3 and
# gives the evals harness the trail it needs.
